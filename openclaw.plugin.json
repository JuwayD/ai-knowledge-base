{
  "id": "ai-knowledge-base",
  "name": "AI Knowledge Base",
  "description": "Native OpenClaw plugin scaffold converted into the ai-knowledge-base plugin workspace.",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "prefix": {
        "type": "string",
        "description": "Prefix added to the sample tool output."
      }
    }
  }
}

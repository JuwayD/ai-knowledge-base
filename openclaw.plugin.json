{
  "id": "ai-knowledge-base",
  "name": "AI Knowledge Base",
  "description": "OpenClaw-native packaging of the AI knowledge-base Codex plugin, including bundled skills and Python CLI scripts.",
  "version": "1.0.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "kbHome": {
        "type": "string",
        "description": "Overrides the default knowledge-base data directory."
      },
      "pythonExecutable": {
        "type": "string",
        "description": "Python interpreter used to execute the bundled scripts."
      }
    }
  }
}
